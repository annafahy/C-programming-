# C-programming-Malloc Example
This program in C uses malloc() to allocate a new data structure for each
data record that you read from the file then stores the pointer returned by malloc in the array of .
